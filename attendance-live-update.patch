--- a/components/records/EditAttendanceModal.tsx
+++ b/components/records/EditAttendanceModal.tsx
@@ -11,7 +11,7 @@
   record: AttendanceRecord | null;
   workspaceId: string;
   memberId: string;
-  onSaved?: (updated: AttendanceRecord) => void;
+  onSaved?: (originalId: string, updated: AttendanceRecord) => void;
 }
 
 function toISODate(iso: string): string {
@@ -91,7 +91,7 @@
     setSaving(false);
 
     if (result.success && result.record) {
-      onSaved?.(result.record);
+      onSaved?.(record.id, result.record);
       onClose();
     } else {
       setError(result.error || "Failed to update record");

--- a/components/members/MemberAttendanceView.tsx
+++ b/components/members/MemberAttendanceView.tsx
@@ -99,9 +99,9 @@
   return streak;
 }
 
-  function handleSaved(updated: AttendanceRecord) {
+  function handleSaved(originalId: string, updated: AttendanceRecord) {
     setRecordsState((prev) =>
-      prev.map((r) => (r.id === updated.id ? updated : r))
+      prev.map((r) => (r.id === originalId ? updated : r))
     );
   }
 

--- a/components/mobile/MemberProfileAttendanceView.mobile.tsx
+++ b/components/mobile/MemberProfileAttendanceView.mobile.tsx
@@ -72,8 +72,8 @@
     .sort((a, b) => new Date(b.date).getTime() - new Date(a.date).getTime())
     .slice(0, 8)
 
-  function handleSaved(updated: AttendanceRecord) {
-    setRecordsState((prev) => prev.map((r) => (r.id === updated.id ? updated : r)))
+  function handleSaved(originalId: string, updated: AttendanceRecord) {
+    setRecordsState((prev) => prev.map((r) => (r.id === originalId ? updated : r)))
   }
 
   return (
