--- a/app/api/cron/fee-reminders/route.ts	2026-09-05 17:32:27.053904479 +0000
+++ b/app/api/cron/fee-reminders/route.ts	2026-09-05 17:32:09.021307436 +0000
@@ -20,18 +20,28 @@
  * sit in the Pending Queue tab waiting for a human to click Send, which
  * getFeeWorklist already surfaces without this job's help.
  *
- * ⚠️ Not fully wired yet: sending a business-initiated WhatsApp message
- * outside the 24h reply window requires an *approved* Meta message
- * template (same constraint the receipt agent hit — see
- * lib/whatsapp/client.ts). There's no approved "fee_reminder" template on
- * this account yet, so FEE_REMINDER_TEMPLATE_NAME below is a placeholder.
- * Until that's approved and swapped in, this job safely no-ops sends (logs
- * + skips) rather than silently failing or spamming free-form text that
- * Meta will reject for members outside the reply window.
+ * Both templates below are Meta-approved (checked in WhatsApp Manager →
+ * Manage templates — green "Approved" status):
+ *
+ *   pre_due_fee_reminder — before_due stage, 4 body variables:
+ *     {{1}} member name, {{2}} workspace/gym name, {{3}} amount (no ₹,
+ *     the template text already has the symbol), {{4}} due date.
+ *
+ *   fee_reminder — overdue stage, 5 body variables: same first four plus
+ *     {{5}} number of days pending.
  */
 
-const FEE_REMINDER_TEMPLATE_NAME = "fee_reminder"; // TODO: swap for the real approved template name once live.
-const FEE_REMINDER_TEMPLATE_APPROVED = false;
+const BEFORE_DUE_TEMPLATE_NAME = "pre_due_fee_reminder";
+const OVERDUE_TEMPLATE_NAME = "fee_reminder";
+const FEE_REMINDER_TEMPLATES_APPROVED = true;
+
+function formatDueDate(dueDate: string): string {
+  return new Date(`${dueDate}T00:00:00+05:30`).toLocaleDateString("en-IN", {
+    day: "numeric",
+    month: "short",
+    year: "numeric",
+  });
+}
 
 export async function GET(request: Request) {
   const authHeader = request.headers.get("authorization");
@@ -67,16 +77,23 @@
 
     if (!AUTO_WHATSAPP_ENABLED) {
       skipped = "AUTO_WHATSAPP_ENABLED is off workspace-wide";
-    } else if (!FEE_REMINDER_TEMPLATE_APPROVED) {
+    } else if (!FEE_REMINDER_TEMPLATES_APPROVED) {
       skipped = "no approved WhatsApp template for fee reminders yet";
     } else {
       for (const item of eligible) {
         if (!item.memberPhone) continue;
-        const result = await sendWhatsAppTemplate(item.memberPhone, FEE_REMINDER_TEMPLATE_NAME, [
-          item.memberName,
-          `₹${item.amount.toLocaleString("en-IN")}`,
-          item.dueDate,
-        ]);
+
+        const templateName = item.reminderStage === "overdue" ? OVERDUE_TEMPLATE_NAME : BEFORE_DUE_TEMPLATE_NAME;
+        const amountParam = item.amount.toLocaleString("en-IN");
+        const dueDateParam = formatDueDate(item.dueDate);
+
+        const bodyParams =
+          item.reminderStage === "overdue"
+            ? [item.memberName, workspaceName, amountParam, dueDateParam, String(item.daysOverdue)]
+            : [item.memberName, workspaceName, amountParam, dueDateParam];
+
+        const result = await sendWhatsAppTemplate(item.memberPhone, templateName, bodyParams);
+
         if (result.success) {
           await supabase.from("reminders").insert({
             workspace_id: workspaceId,
@@ -97,7 +114,6 @@
     }
 
     results.push({ workspaceId, eligible: eligible.length, sent, skipped });
-    void workspaceName;
   }
 
   return NextResponse.json({ ok: true, checked: results.length, results });
--- a/components/automations/FeeReminderView.tsx	2026-09-05 17:32:27.142506121 +0000
+++ b/components/automations/FeeReminderView.tsx	2026-09-05 17:32:18.430103350 +0000
@@ -577,9 +577,9 @@
                 </p>
               )}
               {AUTO_WHATSAPP_ENABLED && (
-                <p className="text-xs text-amber-600">
-                  Auto mode fires from an hourly server job, not from this page being open — needs an approved
-                  WhatsApp template for fee reminders before it can actually send (see cron route comment).
+                <p className="text-xs text-gray-400">
+                  Auto mode fires from an hourly server job, not from this page being open — both the before-due
+                  and overdue WhatsApp templates are approved and live.
                 </p>
               )}
             </>
