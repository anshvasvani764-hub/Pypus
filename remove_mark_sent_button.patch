diff --git a/app/actions/agent.ts b/app/actions/agent.ts
index c24c873..6775650 100644
--- a/app/actions/agent.ts
+++ b/app/actions/agent.ts
@@ -167,32 +167,6 @@ export async function sendAgentReminder({
   return { success: true };
 }
 
-/** Called when the owner has sent the receipt themselves manually (own
- * phone/CRM, since automatic WhatsApp sending is off — see
- * AUTO_WHATSAPP_ENABLED). This is what actually flips a receipt from
- * "Queued" to "Sent" in the Agent log right now. */
-export async function markReceiptSentManually(
-  receiptId: string
-): Promise<{ success: boolean; error?: string }> {
-  const supabase = createServiceClient();
-
-  const { error } = await supabase
-    .from("receipts")
-    .update({
-      whatsapp_sent_at: new Date().toISOString(),
-      whatsapp_status: "sent_manual",
-    })
-    .eq("id", receiptId);
-
-  if (error) {
-    console.error("markReceiptSentManually update error:", error);
-    return { success: false, error: error.message };
-  }
-
-  revalidatePath(`/[app]/agent`, "page");
-  return { success: true };
-}
-
 /** Called right after a payment is marked paid. Uploads the receipt image
  * (generated client-side on canvas) to Supabase Storage, saves the row,
  * and pings the owner on Telegram so they know to send it on WhatsApp
diff --git a/components/agent/AgentPendingView.tsx b/components/agent/AgentPendingView.tsx
index 0d226e7..2cf8445 100644
--- a/components/agent/AgentPendingView.tsx
+++ b/components/agent/AgentPendingView.tsx
@@ -15,7 +15,7 @@ import {
   X as XIcon,
 } from "lucide-react";
 import { PageHeader } from "@/components/layout/PageHeader";
-import { sendAgentReceipt, markReceiptSentManually } from "@/app/actions/agent";
+import { sendAgentReceipt } from "@/app/actions/agent";
 import { AUTO_WHATSAPP_ENABLED } from "@/lib/config/messaging";
 import type { ReceiptWorklistItem, AgentActivityItem } from "@/lib/agent/queries";
 
@@ -164,26 +164,6 @@ export function AgentPendingView({
     void fireSend(key, item);
   }
 
-  // Owner sent the receipt manually (own phone/CRM) — confirm it here so the
-  // row flips from "Queued" to "Sent". This is the primary action right now
-  // since automatic WhatsApp sending is off.
-  async function markSent(key: string, receiptId: string) {
-    setMenuKey(null);
-    setRows((prev) => prev.map((r) => (r.key === key ? { ...r, status: "sending" } : r)));
-
-    const result = await markReceiptSentManually(receiptId);
-
-    setRows((prev) =>
-      prev.map((r) =>
-        r.key === key
-          ? result.success
-            ? { ...r, status: "sent" }
-            : { ...r, status: "failed", error: result.error }
-          : r
-      )
-    );
-  }
-
   function openMemberFees(memberId: string) {
     router.push(`/${workspaceSlug}/members/${memberId}/fees`);
   }
@@ -333,19 +313,6 @@ export function AgentPendingView({
                     : s.label}
                 </span>
 
-                {!AUTO_WHATSAPP_ENABLED && (row.status === "queued" || row.status === "failed") && (
-                  <button
-                    type="button"
-                    onClick={(e) => {
-                      e.stopPropagation();
-                      void markSent(row.key, row.item.receiptId);
-                    }}
-                    className="shrink-0 rounded-lg border border-emerald-200 bg-emerald-50 px-2.5 py-1 text-xs font-medium text-emerald-700 hover:bg-emerald-100"
-                  >
-                    Mark sent
-                  </button>
-                )}
-
                 <div className="flex shrink-0 items-center gap-2">
                   <span className={`h-2 w-2 rounded-full ${s.dot}`} />
                   <span className="relative">
